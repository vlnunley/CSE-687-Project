/*
CSE 687 Project
Contains the definition of the Utils class
*/

#pragma once
#include <queue>
#include <map>
#include "FileManagement.h"

using std::queue;
using std::pair;
using std::map;

class Utils
{
private:

public:
	//Actions
	void sortWords(map<string, vector<int>>& map, string line);
};
